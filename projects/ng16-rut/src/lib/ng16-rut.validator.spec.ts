import { Ng16RutValidator } from './ng16-rut.validator';

describe('Ng16RutValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new Ng16RutValidator();
    expect(directive).toBeTruthy();
  });
});
