import { Ng16RutValueAccessorDirective } from './ng16-rut-value-accessor';

describe('Ng16RutValueAccessor', () => {
  it('should create an instance', () => {
    const directive = new Ng16RutValueAccessor();
    expect(directive).toBeTruthy();
  });
});
