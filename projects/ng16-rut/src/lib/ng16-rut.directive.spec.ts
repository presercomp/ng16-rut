import { Ng16RutDirective } from './ng16-rut.directive';

describe('Ng16RutDirective', () => {
  it('should create an instance', () => {
    const directive = new Ng16RutDirective();
    expect(directive).toBeTruthy();
  });
});
